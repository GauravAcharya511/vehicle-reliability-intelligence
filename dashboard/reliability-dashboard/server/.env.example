# Copy to .env and edit.

# seed = placeholder data (default). postgres = your real pipeline output.
DATA_MODE=seed

# Local Postgres with your macOS login (matches `psql -d vehicle_reliability`).
# Format: postgresql://<your-mac-username>@localhost:5432/vehicle_reliability
DATABASE_URL=postgresql://localhost:5432/vehicle_reliability

PORT=4000
