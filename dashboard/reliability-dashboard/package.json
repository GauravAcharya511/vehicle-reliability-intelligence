{
  "name": "vehicle-reliability-dashboard",
  "version": "1.0.0",
  "description": "React + Node/Express console over the Vehicle Field Reliability pipeline output",
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client && npm install",
    "seed": "node server/seed/generate.js",
    "dev": "concurrently -n api,web -c cyan,magenta \"npm start --prefix server\" \"npm run dev --prefix client\"",
    "build": "npm run build --prefix client"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
